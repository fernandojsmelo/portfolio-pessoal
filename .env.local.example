# Personal Access Token do GitHub, usado em build-time para consultar
# metadados dos projetos (stars, linguagem, última atualização) via
# api.github.com. Eleva o rate limit de 60 para 5.000 req/h.
# Escopo mínimo necessário: public_repo (leitura de repositórios públicos).
GITHUB_TOKEN=
